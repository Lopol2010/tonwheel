export abstract class Consts {
    static max_basis_points = 10000;
}

export abstract class Op {
    static deposit = 1;
    static try_end_round = 2;
    static set_comission_cfg = 3;
}

export abstract class Errors {
    static msg_value_too_small = 400;
}